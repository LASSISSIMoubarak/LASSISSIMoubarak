[project]
name = "mlops_breast_cancer"
version = "0.1.0"
description = "A minimal Python project showing best practices."
readme = "README.md"
requires-python = ">=3.8"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
package-dir = {"" = "src"}
